{
    "version":"0.2.0",
    "configurations": [
        {
            "name": "Debug hello",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/hello",
            "cwd":"${workspaceFolder}",
            "args": []
        }
    ]
}